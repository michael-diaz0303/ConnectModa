apiVersion: apps/v1
kind: Deployment
metadata:
  name: connectmoda-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: NODE_ENV
              value: "staging"
            - name: LOG_LEVEL
              value: "debug"
